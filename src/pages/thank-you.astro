---
const year = new Date().getFullYear();
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" href="/favicon.ico" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Submission Received — Clearline Capital</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;1,300;1,400&family=Inter:wght@300;400;500&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>

    <div class="page">
      <div class="content">
        <a href="/" class="logo" aria-label="Clearline Capital home">
          <img src="/logo.png" alt="Clearline Capital" class="logo-img" />
        </a>

        <div class="divider"></div>

        <p class="eyebrow">Submission Received</p>
        <h1>Thank you for your interest in Clearline Capital.</h1>

        <p class="body">
          All requests are reviewed internally.<br /><br />
          If your profile aligns with our current allocation parameters, a member of our team will contact you directly.
        </p>

        <p class="closing">Discretion is fundamental to our process.</p>

        <a href="/" class="back-link">Return to Site</a>
      </div>

      <footer>
        <p>&copy; {year} Clearline Capital. All rights reserved.</p>
      </footer>
    </div>

  </body>
</html>

<style>
  *, *::before, *::after {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }

  :root {
    --gold:      #c9a96e;
    --gold-dim:  rgba(201, 169, 110, 0.45);
    --dark:      #0c0c0c;
    --off-white: #fafaf8;
    --muted:     #4a4845;
    --serif:     'Cormorant Garamond', Georgia, serif;
    --sans:      'Inter', system-ui, sans-serif;
  }

  html {
    height: 100%;
  }

  body {
    font-family: var(--sans);
    background: var(--off-white);
    color: var(--dark);
    -webkit-font-smoothing: antialiased;
    min-height: 100%;
    display: flex;
    flex-direction: column;
  }

  .page {
    flex: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 4rem 2rem;
    min-height: 100vh;
  }

  .content {
    max-width: 560px;
    width: 100%;
    text-align: center;
  }

  .logo-img {
    height: 52px;
    width: auto;
    display: block;
    margin-inline: auto;
    margin-bottom: 3rem;
  }

  .divider {
    width: 40px;
    height: 1px;
    background: var(--gold);
    margin-inline: auto;
    margin-bottom: 2.5rem;
  }

  .eyebrow {
    font-family: var(--sans);
    font-size: 0.65rem;
    font-weight: 400;
    letter-spacing: 0.4em;
    text-transform: uppercase;
    color: var(--gold);
    margin-bottom: 1.5rem;
  }

  h1 {
    font-family: var(--serif);
    font-size: clamp(1.8rem, 4vw, 2.8rem);
    font-weight: 300;
    line-height: 1.25;
    color: var(--dark);
    margin-bottom: 2.5rem;
  }

  .body {
    font-size: 0.9rem;
    font-weight: 300;
    color: var(--muted);
    line-height: 1.9;
    margin-bottom: 2.5rem;
  }

  .closing {
    font-family: var(--serif);
    font-size: 1rem;
    font-style: italic;
    font-weight: 300;
    color: var(--dark);
    margin-bottom: 3rem;
  }

  .back-link {
    display: inline-block;
    font-family: var(--sans);
    font-size: 0.7rem;
    font-weight: 400;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    text-decoration: none;
    color: var(--dark);
    border: 1px solid var(--dark);
    padding: 0.65rem 2rem;
    transition: background 0.22s ease, color 0.22s ease, border-color 0.22s ease;
  }

  .back-link:hover,
  .back-link:active {
    background: var(--gold);
    border-color: var(--gold);
    color: var(--dark);
  }

  footer {
    margin-top: 4rem;
    text-align: center;
  }

  footer p {
    font-size: 0.68rem;
    letter-spacing: 0.12em;
    color: #aaa;
  }
</style>
